import { describe, it } from 'vitest'
import { defaultLanguageFiles } from '../defaultProjectStruct'

describe('defaultLanguageFiles', () => {
  it('should return the correct default language files', () => {
    console.log(defaultLanguageFiles)
  })
})
